

class Score {
    constructor(data){
        this.id = data.id,
        this.username = data.username,
        this.score = data.score;
    }
}

static getAllScores() {
    return new Promise(async (resolve,reject) => {
        try {
            let 
        }
    })
}
